# Netlify redirects (primary format)
/*    /index.html   200

# Vercel redirects (alternative format)
# /*    /index.html   200

# Additional redirects for other hosting services
# For GitHub Pages, Surge.sh, Firebase Hosting, etc.
# /*    /index.html   200

# API routes (if you have any) - redirect to your backend
# /api/*    https://your-backend-url.com/api/:splat    200

# Static assets - serve directly
# /static/*    /static/:splat    200
